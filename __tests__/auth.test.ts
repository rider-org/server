describe("checking auth suite to make sure it functions properly", () => {
  test("the addition of 1 and 1", () => {
    expect(1 + 1).toBe(2);
  });

  test("the subtraction of 1 and 1", () => {
    expect(1 - 1).toBe(0);
  });
});
